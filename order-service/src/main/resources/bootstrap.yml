spring.cloud.kubernetes.config.name: openshift-spring-boot-demo-config-map

spring:
  cloud.kubernetes.reload:
    enabled: true
    strategy: restart_context

management:
  endpoint:
    restart:
      enabled: true
