spring:
  cloud.kubernetes:
    reload:
      enabled: true
      strategy: restart_context
    config:
      enabled: true
      sources:
            - namespace: bhavesh-dhera-dev
              name: hello-world

management:
  endpoint:
    restart:
      enabled: true
